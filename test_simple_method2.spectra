module test;

struct Point {
    x: int,
    y: int
}

impl Point {
    fn new(x: int, y: int) -> Point {
        Point { x: x, y: y }
    }
    
    fn get_x(&self) -> int {
        self.x
    }
}

fn main() -> int {
    let p = Point::new(3, 4);
    let x = p.get_x();
    x
}
