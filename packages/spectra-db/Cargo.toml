[package]
name = "spectra-db"
version = "0.1.0"
edition = "2021"

[dependencies]
spectra-runtime = { path = "../../runtime" }
rusqlite = { version = "0.32", features = ["bundled"] }

[dev-dependencies]
