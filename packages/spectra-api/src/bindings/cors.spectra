// Public Spectra surface for Phase 23 CORS middleware.
//
// The compiler exposes these declarations as the stable `std.api.cors`
// built-in module. Native behavior is provided by `spectra.api.cors.*`.

module std.api.cors;

pub struct CorsPolicy {
    pub origin_count: int,
}

// Native function declarations are represented in
// compiler/src/semantic/builtin_modules.rs until package-level native extern
// declarations become first-class syntax.
