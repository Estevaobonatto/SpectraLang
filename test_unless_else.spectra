// Teste com else (como o teste 10 original)
module test_unless_else;

fn test_unless(value: int) -> int {
    unless value < 10 {
        return 100;
    } else {
        return 0;
    }
}

fn main() -> int {
    return test_unless(15);
}
