// Test generic enum creation (pattern matching TODO)

module test_enum_match;

enum Option<T> {
    Some(T),
    None
}

fn main() -> int {
    let opt1 = Option<int>::Some(42);
    let opt2 = Option<int>::None;
    let opt3 = Option<float>::Some(3.14);
    
    return 0;
}
