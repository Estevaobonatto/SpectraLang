module mathutils

public func triple(x: int) returns int {
    return x * 3
}

public func sum_to(n: int) returns int {
    let total = 0
    let i     = 0

    while i <= n {
        total = total + i
        i = i + 1
    }

    return total
}
