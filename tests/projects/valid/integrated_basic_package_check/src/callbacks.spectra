module callbacks

public func run_callback(value: int, f: func(int) returns int) returns int {
    return f(value)
}

public func choose_shift(positive: bool, base: int) returns func(int) returns int {
    if positive {
        return | value: int | value + base
    }
    return | value: int | value - base
}
