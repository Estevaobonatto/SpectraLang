// Teste: Código válido - sem erros semânticos
module test_valid;

pub fn main() {
    let x = 10;
    let y = 20;
    let sum = add(x, y);
    let result = x + y;
    return;
}

fn add(a: int, b: int) -> int {
    return a + b;
}

fn multiply(a: int, b: int) -> int {
    let result = a * b;
    return result;
}
