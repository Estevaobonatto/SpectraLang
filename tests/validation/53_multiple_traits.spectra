// Teste: Múltiplos traits implementados
// Testa um tipo implementando vários traits simultaneamente
module test53

// Trait Clone
trait Clone {
    func clone(self) returns Self
}

// Trait Debug
trait Debug {
    func debug(self) returns int
}

// Struct que implementa ambos os traits
record Value {
    data: int
}

impl Clone for Value {
    func clone(self) returns Value {
        return self
    }
}

impl Debug for Value {
    func debug(self) returns int {
        return self.data
    }
}

func main() returns int {
    let v: Value       = Value { data: 42 }
    let copy: Value    = v.clone()
    let debug_val: int = copy.debug()

    return debug_val
    // Deve retornar 42
}
