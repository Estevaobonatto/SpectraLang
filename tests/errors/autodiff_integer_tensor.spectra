module autodiff_integer_tensor

import std.tensor as tensor

public func main() returns int {
    let x = tensor.full(2, 1)
    let y = diff {
        tensor.sum_t(x)
    }
    return tensor.len(y)
}
