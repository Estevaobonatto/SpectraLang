module async_refcell_across_await

record RefCell {
    value: int
}

async func ready() returns int {
    return 1
}

async func bad() returns int {
    let cell   = RefCell { value: 41 }
    let waited = await ready()
    return cell.value + waited
}
