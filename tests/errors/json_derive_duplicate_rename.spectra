module json_derive_duplicate_rename

#[derive(Serialize, Deserialize)]
record Profile {
    #[json(rename = "id")]
    user_id: int,
    #[json(rename = "id")]
    account_id: int,
}

public func main() returns int {
    return 0
}
