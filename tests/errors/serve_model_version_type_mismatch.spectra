module serve_model_version_type_mismatch

import std.serve as serve

public func main() returns int {
    let server = serve.server_new(2)
    serve.server_set_model_version(server, 123)
    return 0
}
