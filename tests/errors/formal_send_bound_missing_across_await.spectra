module formal_send_bound_missing_across_await

async func ready(value: int) returns int {
    return value
}

func keep<T>(value: T) returns int {
    return 1
}

async func bad<T>(value: T) returns int {
    let carried = value
    let waited  = await ready(1)
    return waited + keep(carried)
}
