module formal_send_bound_rejects_non_send

record NonSend {
    value: int
}

func requires_send<T: Send>(value: T) returns int {
    return 1
}

func main() returns int {
    let local = NonSend { value: 7 }
    return requires_send(local)
}
