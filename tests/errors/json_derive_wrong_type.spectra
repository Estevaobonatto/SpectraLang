module json_derive_wrong_type

#[derive(Serialize, Deserialize)]
record Profile {
    #[json(rename = "user_id")]
    id: int,
    name: string,
}

public func main() returns int {
    let decoded: Profile = Profile::from_json("{\"user_id\":\"bad\",\"name\":\"Ada\"}")
    return decoded.id
}
