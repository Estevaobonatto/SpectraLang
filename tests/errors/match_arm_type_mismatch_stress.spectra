module match_arm_type_mismatch_stress

enum Choice {
    Number(int),
    Text,
}

public func main() returns int {
    let choice     = Choice::Text
    let value: int = match choice {
        when Choice::Number(n) then n,
        when Choice::Text then "text"
    }
    return value
}
