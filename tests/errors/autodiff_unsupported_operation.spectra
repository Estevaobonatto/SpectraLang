module autodiff_unsupported_operation

import std.tensor as tensor

public func main() returns int {
    let x = tensor.full_f(2, 1.0)
    let y = diff {
        tensor.sum_t(tensor.tanh_f(x))
    }
    return tensor.len(y)
}
