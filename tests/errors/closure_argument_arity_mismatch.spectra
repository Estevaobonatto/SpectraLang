module closure_argument_arity_mismatch

func apply_pair(f: func(int, int) returns int) returns int {
    return f(1, 2)
}

public func main() returns int {
    let only_one: func(int) returns int = | value: int | value + 1
    return apply_pair(only_one)
}
