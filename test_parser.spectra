module test;

import std.io;
import std.collections;

pub fn main() {
    let x: i32 = 42;
    let message = "Hello, SpectraLang!";
    
    print(message);
    return;
}

fn add(a: i32, b: i32) -> i32 {
    let result = calculate(a, b);
    return result;
}

fn calculate(x, y) {
    return;
}
