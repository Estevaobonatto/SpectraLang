module test_strings

func test_concat() returns string {
    let s1     = "Hello"
    let s2     = " World"
    let result = s1 + s2
    result
}

func test_multi_concat() returns string {
    let greeting = "Hello" + " " + "World" + "!"
    greeting
}
