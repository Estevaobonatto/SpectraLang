module test

func unless_test(x: int) returns int {
    let result = 0

    if not x < 0 {
        result = x * 2
    }

    return result
}

public func main() {
    let r = unless_test(5)
    return
}
