module test

// Test: Match apenas com literais - deve avisar que não é exhaustivo
func test_only_literals() returns int {
    let x = 5
    match x {
        when 1 then 10,
        when 2 then 20,
        when 5 then 50
    }
}

func main() returns int {
    test_only_literals()
}
