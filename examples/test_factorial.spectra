module test

func factorial(n: int) returns int {
    let result = 1
    let i      = 1

    while i <= n {
        result = result * i
        i = i + 1
    }

    return result
}

public func main() {
    let result = factorial(5)
    return
}
