module test

record Point {
    x: int,
    y: int
}

impl Point {
    func new(x: int, y: int) returns Point {
        Point { x: x, y: y }
    }

    func get_x( & self) returns int {
        42 // Simplificado - retorna constante
    }
}

func main() returns int {
    let p = Point::new(3, 4)
    0
}
