module test_tuple_return

func get_pair() returns(int, int) {
    return(10, 20)
}

func main() returns int {
    let result = get_pair()
    let x      = result.0
    let y      = result.1
    x + y
}
