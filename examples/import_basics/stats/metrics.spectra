module stats.metrics;

import calc.arithmetic;

pub fn compute_score(base: int, bonus: int) -> int {
    let boosted = increase(base, bonus);
    let doubled = double(boosted);
    let zero = 0;
    if doubled > 100 {
        return 100;
    }

    if doubled < zero {
        return zero;
    }
    return doubled;
}

pub fn adjust_score(value: int) -> int {
    return half(value);
}
