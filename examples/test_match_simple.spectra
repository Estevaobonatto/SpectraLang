module test

enum Color {
    Red,
    Green,
    Blue
}

func test_match_unit() returns int {
    let color = Color::Red

    return match color {
        when Color::Red then 1,
        when Color::Green then 2,
        when Color::Blue then 3
    }
}

func main() returns int {
    test_match_unit()
}
