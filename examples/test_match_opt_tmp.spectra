module test_match_opt;

fn find_positive(x: int) -> Option<int> {
    if x > 0 { return Option::Some(x); }
    return Option::None;
}

fn main() -> int {
    let answer = match find_positive(4) {
        Option::Some(n) => n,
        Option::None => -1
    };
    return 0;
}
