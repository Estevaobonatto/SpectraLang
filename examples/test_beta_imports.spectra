module test_beta_imports

// Named import: from math.trig import sin, cos
from std.io import println, print

// Alias import: import path as name;
import std.math as math
import std.collections as col

func main() returns int {
    // The imports themselves are the feature being tested —
    // correct parsing of aliased and named import syntax.
    return 0
}
