module test_argv;
import std.env;
import std.io;
import std.convert;

fn main() {
    let count = env_args_count();
    let i = 0;
    while i < count {
        println(env_arg(i));
        i = i + 1;
    }
}
